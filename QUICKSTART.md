# 🚀 快速啟動指南

## 5 分鐘內啟動 Safe-Net 管理系統

### 步驟 1: Firebase Console 設置（3 分鐘）

#### 1.1 設置 Firestore 安全規則

1. 前往 https://console.firebase.google.com/
2. 選擇專案：`safe-net-tw`
3. 左側選單 → **Firestore Database**
4. 點擊 **規則** 標籤
5. 複製貼上以下規則並點擊 **發布**：

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

#### 1.2 啟用 Firebase Authentication

1. 左側選單 → **Authentication**
2. 點擊 **開始使用**
3. 點擊 **電子郵件/密碼**
4. 啟用 **電子郵件/密碼** 選項
5. 點擊 **儲存**

### 步驟 2: 創建測試用戶（2 分鐘）

#### 2.1 在 Firebase Authentication 創建用戶

1. **Authentication** → **Users** → **新增使用者**
2. 創建超級管理員：
   - Email: `admin@safenet.com`
   - Password: `admin123456`
3. **記下生成的 User UID**（例如：`abc123def456`）

#### 2.2 在 Firestore 創建用戶資料

1. 前往 **Firestore Database**
2. 點擊 **開始集合**
3. 集合 ID: `users`
4. 文檔 ID: **使用上面記下的 User UID**
5. 添加以下欄位：

| 欄位名稱 | 類型 | 值 |
|---------|------|-----|
| `email` | string | `admin@safenet.com` |
| `name` | string | `超級管理員` |
| `role` | string | `SUPER_ADMIN` |
| `tenantId` | null | (留空) |
| `phone` | string | `0912-345-678` |
| `isActive` | boolean | `true` |
| `createdAt` | timestamp | (點擊時鐘圖示選擇當前時間) |
| `updatedAt` | timestamp | (點擊時鐘圖示選擇當前時間) |

6. 點擊 **儲存**

### 步驟 3: 啟動應用（30 秒）

```bash
# 確保已安裝依賴
npm install

# 啟動開發伺服器
npm run dev
```

### 步驟 4: 登入測試

1. 開啟瀏覽器前往 http://localhost:3000
2. 使用以下帳號登入：
   - Email: `admin@safenet.com`
   - Password: `admin123456`
3. 成功！🎉

## ✅ 驗證清單

- [ ] Firestore 安全規則已設置
- [ ] Firebase Authentication 已啟用電子郵件/密碼
- [ ] 測試用戶已在 Authentication 創建
- [ ] 測試用戶資料已在 Firestore 創建
- [ ] User UID 與 Firestore 文檔 ID 一致
- [ ] 應用已啟動在 localhost:3000
- [ ] 可以成功登入

## 🐛 常見問題

### "用戶資料不存在"
→ 確認 Firestore `users` 集合中的文檔 ID 與 Firebase Auth 的 User UID 完全一致

### "權限不足"
→ 確認 Firestore 安全規則已設置為 `allow read, write: if true;`

### "找不到此帳號"
→ 確認已在 Firebase Authentication 創建用戶

### 無法連接 Firebase
→ 檢查網路連接和 Firebase 配置（`src/config/firebase.ts`）

## 📚 下一步

完成快速啟動後，您可以：

1. **創建更多測試資料**
   - 添加社區（Tenants）
   - 添加長者（Elders）
   - 添加設備（Devices）

2. **測試即時監聽**
   - 在兩個瀏覽器視窗打開應用
   - 在一個視窗修改資料
   - 觀察另一個視窗自動更新

3. **閱讀完整文件**
   - `README.md` - 專案概覽
   - `FIREBASE_SETUP.md` - 詳細設置指南
   - `MIGRATION_SUMMARY.md` - 技術細節

## 🎯 重要提醒

⚠️ **這是開發環境設置**

- 當前使用的是開放的安全規則
- 在部署到生產環境前，必須更新為基於角色的安全規則
- 參考 `FIREBASE_SETUP.md` 中的生產環境規則

---

**需要幫助？** 查看 `FIREBASE_SETUP.md` 獲取詳細說明
